# 第一批 + 第二批合并测试清单

**日期**: 2026-01-20
**分支**: `merge-upstream-batch1-billing-fixes`
**提交数**: 15 个
**文件修改**: 17 个文件，+521/-44 行

---

## 📋 测试清单

### 🔴 高优先级测试（必须通过）

#### 1. Anthropic 缓存计费测试
- [ ] **场景**: Anthropic 渠道 + `/v1/chat/completions` + prompt caching
- [ ] **测试步骤**:
  1. 配置 Anthropic 渠道
  2. 发送带有 prompt caching 的请求
  3. 检查使用日志中的计费金额
- [ ] **预期结果**:
  - 计费应该基于实际输入 tokens（不减去缓存 tokens）
  - 修复前可能只计费 5.5%，修复后应该计费 100%
- [ ] **验证方式**: 对比修复前后的计费金额，应该有显著提升

#### 2. Auto 分组 Task 扣费测试
- [ ] **场景**: 使用 auto 分组调用 Task 接口
- [ ] **测试步骤**:
  1. 创建 auto 分组的令牌
  2. 调用 `/v1/videos` 或 `/suno` 或 `/kling` 接口
  3. 检查使用日志和配额扣除
- [ ] **预期结果**:
  - 使用日志应该正确记录
  - 配额应该正确扣除
  - 日志中应该显示实际选中的分组（不是 "auto"）
- [ ] **验证方式**: 检查数据库中的 logs 表和用户配额

#### 3. 智普/Moonshot 流式缓存统计测试
- [ ] **场景**: 智普或 Moonshot 渠道 + stream=true + 缓存
- [ ] **测试步骤**:
  1. 配置智普或 Moonshot 渠道
  2. 发送流式请求（stream=true）并启用缓存
  3. 检查使用日志中的缓存 tokens 统计
- [ ] **预期结果**:
  - 缓存 tokens 应该正确统计
  - 计费应该考虑缓存 tokens
- [ ] **验证方式**: 检查日志中的 `cached_tokens` 字段

#### 4. WSS 预扣费测试
- [ ] **场景**: WebSocket 接口预扣费
- [ ] **测试步骤**:
  1. 使用 WebSocket 接口
  2. 检查预扣费逻辑
- [ ] **预期结果**:
  - 预扣费应该正确提取 key
  - 支持小写 bearer 和多个空格
- [ ] **验证方式**: 检查预扣费日志

#### 5. 免费模型预扣费测试
- [ ] **场景**: 免费模型的预扣费逻辑
- [ ] **测试步骤**:
  1. 调用免费模型
  2. 检查预扣费和实际扣费
- [ ] **预期结果**:
  - 预扣费逻辑应该基于分组倍率
  - 免费模型不应该扣费
- [ ] **验证方式**: 检查配额变化

---

### 🟡 中优先级测试（强烈推荐）

#### 6. Gemini 功能测试
- [ ] **场景 6.1**: Gemini function calling
  - 测试 propertyNames 清理
  - 测试 snake_case 字段支持
  - 测试 tool call 转换
- [ ] **场景 6.2**: Gemini 文件上传
  - 测试 image/jpg 文件类型支持
- [ ] **场景 6.3**: Gemini 模型列表获取
  - 测试使用原生 v1beta/models 端点
  - 检查返回的模型列表
- [ ] **预期结果**: 所有 Gemini 功能正常工作
- [ ] **验证方式**: 实际调用测试

#### 7. Claude 调用测试
- [ ] **场景**: Claude 模型调用
- [ ] **测试步骤**:
  1. 通过 `/v1/chat/completions` 调用 Claude
  2. 检查日志渲染
- [ ] **预期结果**: Claude 调用正常，日志正确渲染
- [ ] **验证方式**: 检查响应和日志

#### 8. MiniMax 测试
- [ ] **场景 8.1**: MiniMax 模型列表
  - 检查 MiniMax-M2 系列模型是否可用
- [ ] **场景 8.2**: MiniMax 模型获取
  - 确认 MiniMax 不在 FETCHABLE 列表中
- [ ] **预期结果**: MiniMax-M2 模型可用，但不支持自动获取
- [ ] **验证方式**: 检查模型列表和渠道配置

#### 9. Doubao 测试
- [ ] **场景 9.1**: Doubao video 1.5
  - 测试 Doubao video 1.5 模型
- [ ] **场景 9.2**: Doubao /v1/responses
  - 测试 Doubao /v1/responses 端点支持
- [ ] **预期结果**: Doubao 新功能正常工作
- [ ] **验证方式**: 实际调用测试

#### 10. Ollama 测试（附带功能）
- [ ] **场景**: Ollama 模型列表获取
- [ ] **测试步骤**:
  1. 配置 Ollama 渠道
  2. 获取模型列表
- [ ] **预期结果**: 能够正确获取 Ollama 模型列表
- [ ] **验证方式**: 检查返回的模型列表

---

### 🟢 低优先级测试（回归测试）

#### 11. OpenAI 渠道回归测试
- [ ] **场景**: OpenAI 渠道 + 缓存
- [ ] **预期结果**: OpenAI 缓存计费不受影响（继续减去缓存 tokens）
- [ ] **验证方式**: 对比修复前后的计费逻辑

#### 12. 无缓存场景回归测试
- [ ] **场景**: 所有渠道 + 无缓存调用
- [ ] **预期结果**: 无缓存场景不受影响
- [ ] **验证方式**: 检查计费逻辑

#### 13. 其他渠道回归测试
- [ ] **场景**: Azure, DeepSeek, 其他国内渠道
- [ ] **预期结果**: 其他渠道功能正常
- [ ] **验证方式**: 快速冒烟测试

#### 14. 前端功能测试
- [ ] **场景**: 渠道配置页面
- [ ] **预期结果**:
  - MiniMax 不显示"获取模型"按钮
  - Gemini/Ollama 显示"获取模型"按钮
- [ ] **验证方式**: 检查前端页面

---

## 🚀 部署建议

### 方案 A: 直接合并到 dev（快速）

```bash
# 切换到 dev 分支
git checkout dev

# 合并修复分支
git merge merge-upstream-batch1-billing-fixes

# 推送到远程
git push origin dev
```

**优点**: 快速部署
**缺点**: 风险较高，建议先在测试环境验证

---

### 方案 B: 先部署到测试环境（推荐）

```bash
# 推送当前分支到远程
git push origin merge-upstream-batch1-billing-fixes

# 在测试环境部署这个分支
# docker pull your-registry/new-api:merge-upstream-batch1-billing-fixes
# 或者本地构建测试

# 运行上述测试清单

# 测试通过后再合并到 dev
git checkout dev
git merge merge-upstream-batch1-billing-fixes
git push origin dev
```

**优点**: 风险可控，可以充分测试
**缺点**: 需要额外的测试环境

---

### 方案 C: 创建 Pull Request（最安全）

```bash
# 推送当前分支到远程
git push origin merge-upstream-batch1-billing-fixes

# 在 GitHub 创建 PR: merge-upstream-batch1-billing-fixes -> dev
# 进行 code review
# 在 PR 中运行 CI/CD 测试
# 审核通过后合并
```

**优点**: 最安全，有 code review 和 CI/CD
**缺点**: 流程较长

---

## ⚠️ 注意事项

### 1. 计费相关修复的重要性
- **Anthropic 缓存计费修复**是最关键的，直接影响收入
- 建议优先测试所有计费相关功能
- 部署后密切监控计费数据

### 2. 潜在风险点
- **Anthropic 缓存计费**: 修改了核心计费逻辑，需要仔细验证
- **Auto 分组**: 修改了 Task Relay 流程，需要测试各种 Task 接口
- **Gemini 模型获取**: 新增了 Ollama 支持，可能影响现有逻辑

### 3. 回滚方案
如果出现问题，可以快速回滚：

```bash
# 回滚到合并前的状态
git checkout dev
git reset --hard <合并前的commit-hash>
git push origin dev --force

# 或者使用 revert
git revert -m 1 <merge-commit-hash>
git push origin dev
```

### 4. 监控指标
部署后需要监控：
- **计费金额**: 是否有异常波动
- **错误率**: API 调用错误率
- **日志记录**: 使用日志是否正常记录
- **用户反馈**: 是否有用户报告计费问题

---

## 📊 预期收益

### 计费修复收益
- **Anthropic 缓存计费**: 挽回 94.5% 收入损失
- **Auto 分组扣费**: 挽回视频/音乐接口漏计费
- **流式缓存统计**: 挽回缓存 tokens 漏计费
- **预计总收益**: 10-20% 收入提升

### 功能增强收益
- **Gemini 稳定性**: 减少 Gemini 调用失败率
- **Claude 兼容性**: 提升 Claude 调用体验
- **国内渠道**: 支持更多模型和功能

---

## 📝 测试记录

### 测试环境信息
- **环境**: _______________
- **版本**: _______________
- **测试人**: _______________
- **测试日期**: _______________

### 测试结果汇总
- [ ] 所有高优先级测试通过
- [ ] 所有中优先级测试通过
- [ ] 回归测试通过
- [ ] 无阻塞性问题

### 发现的问题
1. _______________
2. _______________
3. _______________

### 测试结论
- [ ] ✅ 可以合并到 dev
- [ ] ⚠️ 需要修复问题后再合并
- [ ] ❌ 不建议合并，需要重新评估

---

**文档版本**: 1.0
**最后更新**: 2026-01-20
